import java.util.stream.IntStream

class WrappedSpace (max: Point) : Space{

    override fun move (currentPosition: Point, vector: Int): Point{


        return
    }
    override fun fill(density: Int, random: IntStream): Collection<Point>{

        return
    }
}